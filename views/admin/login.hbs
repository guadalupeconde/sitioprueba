<div class="conteiner" style="margin: 50px auto;">
    <div class="row">
        <div class="col">
            <form action="/admin/login" method="post">
                <div class="form-group">
                    <label for="usuario">Usuario</label>
                    <input type="text" aria-describedby="usuario" name="usuario">
                </div>
                <div class="form-group" style="margin: 50px auto;">
                    <label for="password">Clave</label>
                    <input type="password" aria-describedby="user" id="password" name="password">
                </div>
                <button type="submit" class="btn btn-primary">Enviar</button>
            </form>
            {{#if error}}
            <p>Usuario o contraseña incorrecta</p>
            {{/if}}
        </div>
    </div>
</div>